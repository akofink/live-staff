# Live Staff Attended Release Evidence

- Build SHA: `21af1f889ac3473fac2fe6511cf65eb09f442dfa`
- App URL: https://live-staff.akofink.com/
- Started: 2026-07-22T22:40:00Z
- Updated: 2026-07-22T23:00:00Z
- Device: MacBook Air \(M2, 16 GB\)
- OS: macOS 26.5.2 \(25F84\)
- Browser: Safari 26.5.2 \(21624.2.5.11.8\)
- Input route: Built-in MacBook Air microphone at 44100 Hz; no wired, Bluetooth, or external route tested
- Viewport: Desktop Safari, 800 CSS px reported by WebDriver; 200 percent zoom and physical mobile layouts not established
- Assistive technology: Safari WebDriver DOM inspection only; VoiceOver was not used
- Sustained duration: 20 minutes
- Battery: 80 percent, discharging to 61 percent, discharging
- Thermal observation: No operating-system thermal warning was observed during the short session; battery change includes unrelated development and browser activity and is not attributed to Live Staff

This report contains attended human observations. No result was inferred automatically.

| Area | Instruction | Attended and unavailable scenarios recorded | Result | Notes and limitations |
| --- | --- | --- | --- | --- |
| Layout | Inspect portrait and landscape at a CSS viewport of at least 320 px, then 200% zoom or the platform large-text setting. Confirm no required control, status, history, or staff alternative is clipped or obscured. | yes | blocked | Safari reported an 800 CSS px desktop viewport with no document-level horizontal overflow. Direct visual inspection at 200 percent zoom, 320 CSS px, portrait, and landscape was not completed. |
| Microphone | From an idle page, activate Start listening. Confirm this user action causes the only permission prompt and listening starts after permission is granted. | yes | pass | A trusted Safari WebDriver click on Start caused the only microphone prompt. The operator selected Allow. The app then displayed Listening locally at 44100 Hz and live pitch content began updating. |
| Pitch and history | Use a named live voice, instrument, or generated acoustic tone. Record expected and displayed notes in Notes, and confirm current note and recent history update together. Do not generalize accuracy beyond observations. | yes | blocked | The current staff text, current note, and bounded 10-second history updated continuously from the built-in microphone. The source was uncontrolled room input rather than a named voice, instrument, or generated acoustic tone, so pitch accuracy is not claimed and this row remains blocked. |
| Transposition | Select B-flat trumpet while using the same concert source. Confirm the written note is one whole step above concert pitch and the pitch reference identifies the concert note. | yes | blocked | Changing from concert pitch to B-flat trumpet immediately changed the same live input presentation from concert E3 to written G-flat3 and identified Written pitch for B-flat trumpet in text and the grand-staff label. Because the uncontrolled source was not independently known, this demonstrates immediate written-pitch presentation but does not validate note accuracy. |
| Calibration | While listening in a quiet room, run room-noise calibration. Confirm understandable status, session-only behavior, and continued response to a louder pitched source. | yes | pass | Calibration completed during active capture and reported Room noise calibrated locally for this listening session. Listening continued afterward. The session-only wording was present; no detector-accuracy conclusion is drawn. |
| Filters | Add and edit a filter, compare enabled and bypassed behavior, reset it, and verify all controls remain operable without a pointer. | yes | blocked | One band-stop filter was added with named enabled, type, frequency, width, reduction, remove, bypass, and reset controls. Bypass was toggled and the monitor reported that pitch detection received raw input while bypassed. Filter creation required a scripted click after WebDriver element clicks did not activate it, and no named source supported an audible enabled-versus-bypassed comparison, so keyboard operability and effect remain blocked. |
| Signal monitor | Enable the opt-in waveform and spectrum monitor while listening, inspect it, then disable it. Confirm the app remains responsive and the diagnostic display stops immediately. | yes | pass | The opt-in monitor was enabled during active capture and displayed two local canvases, waveform and raw spectrum/filter response text, and a live level. Disabling it removed both canvases immediately while listening remained active. |
| Microphone | Stop listening and confirm the browser or operating-system microphone indicator clears. | yes | blocked | Stop changed the app to Listening stopped. Microphone resources were released and restored Start listening. The operator could not attend the macOS microphone indicator, so operating-system track release is not independently passed. |
| Lifecycle | While listening, background or switch away from the browser, then return. Confirm the app explains the pause and one explicit action resumes without a duplicate permission request. | yes | pass | Switching to Chrome while Safari captured changed Safari to Listening paused while the page was in the background. Resume listening to continue. Returning Safari to the foreground and activating Resume restored Listening locally at 44100 Hz without another permission prompt. |
| Lifecycle | While listening, lock and unlock the device. Record the resulting state and whether one clear recovery action works. | yes | blocked | The Mac was not locked during active capture. Operator step: start Safari listening, lock and unlock macOS, record the resulting status, then perform the single offered recovery action. |
| Lifecycle | Cause an available operating-system audio interruption, such as a call or another audio session. Record the resulting state and recovery behavior. | yes | blocked | No controllable operating-system audio interruption was performed. Operator step: cause an available audio-session interruption during capture and record state and recovery without inferring phone behavior from desktop. |
| Lifecycle | Revoke site microphone permission during or between sessions. Confirm the error and recovery guidance are accurate and no microphone remains active. | yes | blocked | Permission was legitimately granted, but it was not revoked during or between sessions. Operator step: revoke this site's microphone permission in Safari settings, confirm the app guidance and inactive indicator, then restore only through normal browser UI. |
| Lifecycle | Change among each available built-in, wired, Bluetooth, or external input route and remove an external input. Record unsupported scenarios as blocked, not passed. | yes | blocked | Only the built-in microphone was available. No wired, Bluetooth, external-input removal, or active route-switch claim is made. |
| Accessibility | Navigate from page start without a pointer. Confirm logical focus order, visible focus, native names, units, values, disabled states, and disclosure states. | yes | blocked | WebDriver key input reached the primary button and native names, values, disabled states, and disclosure states were present in DOM inspection. Direct visible-focus inspection, complete pointer-free traversal, filter editing, and 200 percent zoom were not attended, so this is not a keyboard pass. |
| Accessibility | With the named VoiceOver or TalkBack configuration, confirm idle, permission, listening, silence or uncertain input, detected note, denial, device loss, and recovery messages are announced once and remain understandable. Confirm raw SVG is not read. | yes | blocked | The operator could not complete VoiceOver. DOM text exposed status, current written pitch, staff, and bounded history, but DOM or WebDriver inspection does not establish narration or prove raw SVG is suppressed. Operator step: enable VoiceOver and navigate idle, permission, listening, silence, note, denial, device-loss, and recovery states without a pointer. |
| Privacy | Inspect the browser network log from before Start through Stop and local preference changes. Fail for any audio, detection, preference, profile, analytics, telemetry, or third-party executable request. Record request observations without private URLs or identifiers. | yes | blocked | The operator could not attend Safari Web Inspector Network from a cleared log through Start, active capture, preference changes, and Stop. Operator step: clear Network before Start, exercise filters, monitor, calibration, and Stop, and fail on any audio, detection, preference, analytics, telemetry, or third-party executable request. |
| Sustained performance | Run at least 30 minutes with filters and monitor enabled. Record duration, battery start/end, thermal observation, memory when available, responsiveness, and any dropped or frozen display. | yes | blocked | Active capture lasted less than 30 minutes and the monitor was enabled only briefly. Battery moved from 80 to 61 percent while discharging during combined development and browser work; this is not attributable battery evidence. No active-memory, dropped-update, frozen-display, or sustained thermal claim is made. |
