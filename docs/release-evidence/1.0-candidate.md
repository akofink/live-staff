# 1.0 Candidate Support And Evidence

## Status

This record is partial and does not satisfy issue [#71](https://github.com/akofink/live-staff/issues/71).
It refreshes automated and currently attributable evidence for deployed commit `ded54f73984fac1a1f709cec826089c960f50a37`.
That commit is `origin/main` after pull request [#113](https://github.com/akofink/live-staff/pull/113).
Live Staff remains an unversioned preview at package version `0.0.0`.

Issue [#77](https://github.com/akofink/live-staff/issues/77) still blocks credible 1.0 detector accuracy claims.
Issue [#82](https://github.com/akofink/live-staff/issues/82) still has no validated lossless/AAC fixture set; the [capture kit](../fixture-capture-kit.md) and [remaining matrix](82-remaining-capture-matrix.md) prepare operator work without adding recordings.
Real iOS or iPadOS Safari, Android, screen-reader, sustained performance, thermal, interruption, active-capture privacy-network, and audio-route evidence is still required on this deployed commit.

Historical attended desktop reports remain attributable only to the SHAs they record.
They are not current evidence for `ded54f7`.
No physical-device or screen-reader result was collected in this refresh.

## Evidence Labels

- **Automated** means a repeatable local or CI check with browser or audio interfaces replaced where the test requires control.
- **Emulated viewport** means desktop headless Chromium rendered at specified CSS dimensions; it is not physical mobile-browser evidence.
- **Desktop browser inspection** means production or a local build was inspected in headless Chromium on the named host; it is not an attended Chrome, Safari, or screen-reader session.
- **Physical device** means an identified device and operating system were used directly by a person.
- **Historical attended** means a human attestation exported from the attended harness for a named older SHA.

## Current Deployed Commit

| Item | Value |
| --- | --- |
| Evidence date | 2026-09-20 |
| Commit | `ded54f73984fac1a1f709cec826089c960f50a37` |
| Production URL | `https://live-staff.akofink.com/` |
| Pages last-modified | 2026-09-19T23:51:20Z |
| Production document assets | `/assets/index-BWP3Zlh-.js` and `/assets/index-Ce_MEuRu.css`, matching the local `ded54f7` production build |
| Automated host | WSL2 Ubuntu 24.04.5 LTS |
| Node | 24.15.0 |
| Playwright | 1.62.1 |
| Headless Chromium | Google Chrome for Testing 151.0.7922.34 |
| Installed desktop Chrome on this host | 146.0.7680.164; not used as attended evidence |

This host is not macOS and is not a physical phone or tablet.
Do not describe it as a supported user configuration.

## Support Matrix

Only configurations with current passing physical-device evidence may be described as supported.
The current matrix therefore records no supported physical configurations.

| Configuration | Evidence type | Result | Scope and limitation |
| --- | --- | --- | --- |
| Headless Chromium 151 on Ubuntu 24.04.5 WSL2 | Automated local gate | Pass for automated rows below | Controlled unit, privacy, fixture-regression, and Playwright checks on `ded54f7`. This is not attended Chrome, Safari, or physical-device evidence. |
| Chromium at 320 by 720 CSS px | Automated emulated viewport | Pass | Playwright verified filter editing and the opt-in monitor without horizontal overflow. This is not Android, iOS, touch, browser-chrome, or physical-pixel evidence. |
| Chromium at 390 by 844 and 1440 by 900 CSS px | Automated emulated viewport | Pass | Playwright verified the idle core experience remains within the first viewport without horizontal overflow. |
| Production custom domain initial load | Automated privacy inspection | Pass | `npm run verify:production-privacy` on 2026-09-20 reported no known telemetry or third-party executable request. The check does not start listening and does not prove the absence of transmission during active microphone capture. |
| Safari on iPhone or iPad | None | Required | No device was used in this refresh. |
| Chrome on representative Android hardware | None | Required | No device was used in this refresh. |
| Current desktop Safari on `ded54f7` | None | Required | Prior Safari capture evidence is historical for `21af1f8` only. |
| Current desktop Chrome on `ded54f7` | None | Required | Prior Chrome sessions on `04da1c2` and `21af1f8` did not obtain active capture and are not this commit. |
| VoiceOver or TalkBack | None | Required | No assistive technology was attached. |

## Automated Evidence

The following commands passed after a clean `npm ci` on `ded54f7` at this host on 2026-09-20:

| Command | Result |
| --- | --- |
| `npm run lint` | Pass |
| `npm test` | Pass, 23 files and 121 tests |
| `npm run build` | Pass, initial JavaScript 69.0 KB gzip against a 100 KB budget |
| `npm run test:privacy` | Pass, 3 tests |
| `npm run test:fixture-manifest` | Pass |
| `npm run verify:privacy` | Pass for the built `dist` artifact |
| `npm run verify:production-privacy` | Pass for the production custom domain |
| `npm run evaluate:fixtures` | Pass against the existing piano regression policy; this is not an accuracy claim |
| `npm run evaluate:performance` | Pass, 15 Playwright tests |

The fixture evaluator on this commit produced an independent `test-results/fixture-evaluation.json` with status `complete`.
In-range fixtures: 10.
Matching fixtures: 3 (`a3.m4a`, `bb1.m4a`, `bb2.m4a`).
Emitted in-range estimates: 31.
In-range octave errors: 20.
That matches the reviewed floor of at least 3 matching fixtures, at least 31 estimates, and at most 20 octave errors.
C1 and F1 remain out-of-range observations.
These counts do not support a general or real-device accuracy statement.

The automated browser suite covers the following controlled behavior:

- Start, stop, startup cancellation, one microphone request, resource release, suspended-context resume, and simulated track loss.
- Deterministic C4 analyzer samples displayed as written D4 for B-flat trumpet, with accessible current-note and 10-second history text.
- Deterministic A3 analyzer samples routed to the bass staff and retained as bounded history after stop.
- Local CSV, JSON, and plain-text export of frozen stabilized history after an explicit action, without a network URL.
- Room-calibration availability and the below-threshold result with a synthetic silent stream.
- Four bounded band-stop filters, keyboard edits, bypass availability, reset, 44 CSS px remove controls, and no 320 CSS px overflow.
- Signal-monitor opt-in, no spectrum reads before opt-in, at most one read per 100 ms, one microphone request, immediate analysis stop after disable, and no 320 CSS px overflow.
- A listening-status update within 100 ms in headless Chromium.
- Offline continued use after initial load without a service worker, Cache Storage, or requests from listening and preference changes.

These tests establish application behavior under controlled browser objects.
They do not establish operating-system permission UI, backgrounding, screen lock, interruption, route switching, physical microphone cleanup, mobile power use, screen-reader announcement, or detector accuracy on a live source.

## Production Inspection

On 2026-09-20, headless Chromium loaded `https://live-staff.akofink.com/` for the production privacy verifier.
The document referenced the same content-hashed entry JavaScript and CSS as the local `ded54f7` build.
The verifier reported no known telemetry endpoint and no third-party executable request during initial load.
The check does not click Start, does not request microphone permission, and does not inspect an accessibility tree.

Do not carry forward the July 18 `b31b95a` CDP accessibility-tree notes as current production evidence.
That inspection predated later notation, history-export, and dependency changes.

## Historical Attended Evidence

Keep these reports as SHA-bound limitations.
Do not treat them as passing evidence for `ded54f7`.

| Report | SHA | Result worth retaining |
| --- | --- | --- |
| [attended-04da1c2-chrome-macos.md](attended-04da1c2-chrome-macos.md) | `04da1c2` | Desktop Chrome profile denied microphone permission. Active-audio and active-capture privacy rows remain missing. |
| [attended-21af1f8-chrome-macos.md](attended-21af1f8-chrome-macos.md) | `21af1f8` | Start entered requesting, then capture stayed denied after the operator allowed Chrome microphone permission. No active-audio claim. |
| [attended-21af1f8-safari-macos.md](attended-21af1f8-safari-macos.md) | `21af1f8` | Built-in microphone capture at 44.1 kHz, notation and history updates, written-pitch presentation, calibration, filters, monitor, background pause, and app-level stop were exercised. Accuracy, OS indicator release, complete keyboard and zoom, VoiceOver, privacy network, routes, and 30-minute sustained behavior remained blocked. The source was uncontrolled room input. |

Those Safari observations also predate the shipped local history-export control from issue [#97](https://github.com/akofink/live-staff/issues/97).
Retest export, capture, and accessibility on the current production commit rather than extending the old rows.

## Known Blockers And Limitations

- Issue [#77](https://github.com/akofink/live-staff/issues/77) remains open.
- The fixture threshold prevents regression on one frozen piano AAC set but does not support a general accuracy statement.
- Issue [#82](https://github.com/akofink/live-staff/issues/82) has tooling and an operator kit, but every capture-matrix cell is still remaining.
- No current physical browser configuration has passing evidence in this record.
- No VoiceOver, TalkBack, NVDA, JAWS, or other screen reader was used on `ded54f7`.
- No sustained active capture measured memory growth, battery drain, dropped updates, or thermal behavior on `ded54f7`.
- No background, screen-lock, phone-call, OS interruption, permission-revocation, wired-route, Bluetooth-route, or external-microphone-loss scenario was performed on real hardware for `ded54f7`.
- No live voice, generated acoustic tone, piano, or transposing instrument was evaluated in a production browser for `ded54f7`.

## Operator Checklists

Record the date, production commit, device model, OS version, browser version, input route, duration, result, and limitation for every checklist run.
Attach screenshots only after checking that they do not expose notifications, account data, device identifiers, or other private information.
Use the [developer-only attended evidence harness](../attended-release-evidence.md) against `https://live-staff.akofink.com/` at commit `ded54f7`, or a later deployed SHA that has been re-identified.
The harness does not replace any physical test and never infers a pass.

For detector fixture capture, use the [#82 capture kit](../fixture-capture-kit.md) instead of these device checklists.

### iPhone Or iPad Safari

- Record the deployed commit before evaluating accuracy; do not claim accuracy while issue #77 is open.
- Load production online, then verify the idle staff, primary control, detected-pitch text, setup disclosure, history export, and privacy copy at the device's narrow portrait width and in landscape.
- Start only from the button, allow microphone access in Safari UI, play voice or a generated acoustic tone, and record the displayed and expected concert pitch without generalizing beyond tested notes.
- Select B-flat trumpet and verify the written note is a whole step above the same concert source; confirm history and accessible text update together.
- Add and edit a filter, compare bypass, calibrate in quiet, and enable then disable the monitor.
- Stop and verify the browser microphone indicator clears.
- Repeat after background and resume, screen lock and unlock, an OS audio interruption, permission revocation, wired route change, Bluetooth route change, and external-input removal when hardware permits.
- Run at least 30 minutes with filters and monitor enabled; record responsiveness, memory information if Safari Web Inspector exposes it, battery change, thermal observation, and any dropped or frozen display.
- Inspect Safari Web Inspector network traffic from before Start through Stop; fail the check for any audio, detection, preference, profile, analytics, or third-party executable request.

### Android Chrome

- Record the deployed commit before evaluating accuracy; do not claim accuracy while issue #77 is open.
- Record the phone model, Android version, Chrome version, physical display mode, and whether the CSS viewport is at least 320 px.
- Repeat the core, transposition, history, export, calibration, filters, bypass, monitor, stop, and privacy-network flows from the iOS checklist.
- Repeat after app switching, screen lock, OS audio interruption, site-permission revocation, wired route change, Bluetooth route change, and external-input removal when supported.
- Run at least 30 minutes with filters and monitor enabled; record Chrome task memory when available, battery change, thermal observation, responsiveness, and display continuity.
- Inspect remote DevTools network traffic from before Start through Stop and fail on prohibited transmission.

### Screen Reader And Keyboard

- Use a named screen reader and browser combination on physical desktop or mobile hardware.
- Navigate from page start without a pointer and confirm logical focus order, visible focus, native control names, units, values, disabled state, and disclosure state.
- Confirm idle, requesting-permission, listening, silence or uncertain input, detected note, permission denial, device loss, recovery, and history-export messages are announced once and remain understandable.
- Confirm the visual staff is not read as raw SVG content and that current note, written or concert context, staff, and recent history are available as text.
- Edit all filter control types, toggle bypass, perform calibration, enable and disable diagnostics, and export history without a pointer.
- Repeat at 200 percent zoom or the platform's large-text setting and confirm no control or status is clipped or obscured.

### Release Decision

- Do not close issue #71 while any required physical-device, screen-reader, sustained-performance, or privacy-network row remains missing.
- Do not close issue #82 while the remaining capture matrix has unsupported gaps that were not logged, or while any supported cell remains uncaptured.
- Do not claim supported iOS, Android, Safari, screen-reader, thermal, route-switch, or real-source accuracy behavior from automated Chromium evidence.
- Do not cut 1.0 until issue #77 is merged, the affected deployed commit is retested, issue #71 rows exist for that commit, and the release review accepts the resulting limitations.
